import type { Project } from './types';

export const PROJECTS: Project[] = [
  // ---------------- Beginner ----------------
  {
    id: 'pr-personal-site', tier: 'beginner', skills: ['building', 'prompting'],
    title: 'Personal website',
    summary: 'A single-page static site with an about section, project list, and contact link.',
    requirements: ['Responsive, mobile-first', 'Dark mode by default', 'No framework — plain HTML/CSS/JS', 'Deployable as static files'],
    constraints: ['No build step', 'No external CSS frameworks', 'Lighthouse performance ≥ 95'],
    suggestedWorkflow: ['Spec the sections and content', 'Build the HTML structure', 'Style mobile-first, then desktop', 'Add a theme that respects prefers-color-scheme', 'Verify with a Lighthouse run'],
    hiddenTests: ['Renders with no console errors', 'No horizontal scroll at 375px width', 'All links have href values', 'Contrast passes WCAG AA'],
    evaluationCriteria: ['Loads instantly', 'Readable on a phone', 'Semantic HTML', 'One-command local preview'],
    hints: ['Ask Claude to take a mobile screenshot and compare', 'Keep it one file if you can'],
    solutionStrategy: 'Smallest possible spec, build in one pass, verify visually at two widths and with Lighthouse. This project is about the loop, not the code.',
    reflectionQuestions: ['What did Claude assume about your content that you had to correct?', 'How would you verify the mobile layout without looking?'],
  },
  {
    id: 'pr-calculator', tier: 'beginner', skills: ['building', 'testing'],
    title: 'Calculator',
    summary: 'A working calculator with keyboard support and correct operator precedence.',
    requirements: ['Add, subtract, multiply, divide, percent, sign toggle', 'Keyboard input', 'Handles divide-by-zero gracefully', 'Chained operations'],
    constraints: ['Pure functions for the math, unit tested', 'No eval()'],
    suggestedWorkflow: ['TDD the expression evaluator first', 'Then the UI', 'Then keyboard bindings'],
    hiddenTests: ['1 + 2 * 3 = 7 (or documents its calculator-style behavior)', '5 / 0 shows an error, not Infinity or a crash', 'Backspace and clear work', 'Rapid input does not desync display and state'],
    evaluationCriteria: ['Math is correct and tested', 'No eval', 'Keyboard and click parity'],
    hints: ['Decide up front: precedence or immediate-execution calculator? State it.', 'The evaluator is where the tests belong'],
    solutionStrategy: 'Separate the math (pure, tested) from the DOM. Write evaluator tests first — they double as the spec for "what kind of calculator is this".',
    reflectionQuestions: ['Where did you need to make a product decision Claude could not make for you?', 'What edge cases did the hidden tests catch that you missed?'],
  },
  {
    id: 'pr-quiz', tier: 'beginner', skills: ['building', 'testing'],
    title: 'Quiz app',
    summary: 'A multi-question quiz with scoring, a results screen, and a retry option.',
    requirements: ['Questions from a data file, not hardcoded in components', 'Progress indicator', 'Score + per-question review at the end', 'Retry resets state cleanly'],
    constraints: ['Quiz content is a separate module', 'No backend'],
    suggestedWorkflow: ['Model the content type', 'Build the question flow', 'Add scoring and review', 'Test the state machine'],
    hiddenTests: ['Answering all questions produces the correct score', 'Retry fully resets (no leaked answers)', 'Progress indicator matches actual position', 'Empty question list shows a sensible empty state'],
    evaluationCriteria: ['Content/UI separation', 'Correct scoring', 'Clean reset', 'Handled empty state'],
    hints: ['This mirrors how this Academy separates content from UI', 'Test the reset explicitly — it is where bugs hide'],
    solutionStrategy: 'Content model first. The quiz is a small state machine; test its transitions.',
    reflectionQuestions: ['Why keep quiz content out of the components?', 'What would break if you added a timer later?'],
  },
  {
    id: 'pr-todo', tier: 'beginner', skills: ['building', 'testing', 'git'],
    title: 'Todo application',
    summary: 'CRUD todos with filtering, persistence, and keyboard-friendly interaction.',
    requirements: ['Add, edit, complete, delete', 'Filter: all / active / done', 'Persist to localStorage', 'Keyboard: add on Enter, edit on double-click, Esc to cancel'],
    constraints: ['Wrap all storage access in try/catch', 'Render correctly with no stored value'],
    suggestedWorkflow: ['Spec the model', 'CRUD + filter', 'Persistence layer with error handling', 'Keyboard interactions', 'Test the storage layer against a broken/empty store'],
    hiddenTests: ['A corrupted localStorage value does not crash the app', 'Filters show the right counts', 'Editing to empty either cancels or deletes (documented)', 'Deleting the last item shows an empty state'],
    evaluationCriteria: ['Resilient persistence', 'Correct filtering', 'Keyboard parity', 'Empty and error states handled'],
    hints: ['Ask Claude: "what happens if localStorage throws?"', 'This is the first project where a feature branch is worth it'],
    solutionStrategy: 'The storage layer is the risky part — isolate and test it. Everything else is straightforward CRUD.',
    reflectionQuestions: ['What could go wrong with persistence, and did you verify each?', 'Would you trust Claude to add sync-to-a-server autonomously? What would you need first?'],
  },

  // ---------------- Intermediate ----------------
  {
    id: 'pr-habit', tier: 'intermediate', skills: ['building', 'testing', 'architecture'],
    title: 'Habit tracker',
    summary: 'Track daily habits with streaks, a calendar heatmap, and weekly stats.',
    requirements: ['Create habits, mark done per day', 'Streak calculation (current + longest)', 'Calendar heatmap view', 'Weekly completion stats', 'Data export/import as JSON'],
    constraints: ['Streak logic is pure and unit tested', 'Timezone-safe date handling', 'Works offline'],
    suggestedWorkflow: ['ADR: how are dates and "today" represented?', 'Model + streak engine (TDD)', 'Views', 'Export/import', 'Adversarial date tests'],
    hiddenTests: ['Streak breaks correctly across a missed day', 'DST transition does not create or drop a day', 'Longest streak survives export → import', 'Marking done twice in a day is idempotent'],
    evaluationCriteria: ['Correct streaks including edge dates', 'Timezone safety', 'Lossless export/import', 'Clean architecture doc'],
    hints: ['Dates are the whole difficulty — write the ADR', 'Ask for tests around DST and month boundaries explicitly'],
    solutionStrategy: 'Decide the date model first and write it down. The streak engine is pure logic — TDD it hard, especially boundaries.',
    reflectionQuestions: ['Which date bug would you never have found without asking for edge-case tests?', 'What information did Claude need from you to get the date model right?'],
  },
  {
    id: 'pr-budget', tier: 'intermediate', skills: ['building', 'testing', 'debugging'],
    title: 'Budgeting application',
    summary: 'Categorized transactions, monthly budgets, and over-budget warnings.',
    requirements: ['Add transactions with category, amount, date', 'Set monthly budget per category', 'Dashboard: spent vs budget per category, month total', 'Recurring transactions', 'CSV import of transactions'],
    constraints: ['Money as integer cents, never floats', 'Import is validated and reports bad rows', 'Deterministic month rollover'],
    suggestedWorkflow: ['Model money and categories', 'Transaction CRUD + recurring expansion', 'Budget math', 'CSV import with validation', 'Test money math and import edge cases'],
    hiddenTests: ['0.1 + 0.2 style float errors are impossible (cents)', 'CSV with a malformed row imports the good rows and reports the bad one', 'Recurring transaction crossing a month boundary lands in the right month', 'Deleting a category reassigns or blocks with a clear message'],
    evaluationCriteria: ['No floating-point money bugs', 'Robust import', 'Correct budget math', 'Sensible category deletion behavior'],
    hints: ['Insist on integer cents in the prompt and CLAUDE.md', 'Feed Claude a deliberately messy CSV to test import'],
    solutionStrategy: 'Money-as-cents is non-negotiable — put it in CLAUDE.md. Import validation and month math are where the bugs live; test them adversarially.',
    reflectionQuestions: ['Why is float money a classic bug, and how did you prove it is absent?', 'How did you verify the import without a real bank file?'],
  },
  {
    id: 'pr-recipes', tier: 'intermediate', skills: ['building', 'codebase', 'testing'],
    title: 'Recipe application',
    summary: 'Browse, search, scale, and shop for recipes.',
    requirements: ['Recipe list + detail', 'Fuzzy search by name and ingredient', 'Scale servings (adjusts quantities)', 'Generate a combined shopping list from selected recipes', 'Unit-aware quantity handling'],
    constraints: ['Search tolerates typos', 'Scaling handles fractions and unit conversion sensibly', 'Shopping list merges like ingredients'],
    suggestedWorkflow: ['Model recipes, ingredients, units', 'Search', 'Scaling engine (TDD)', 'Shopping list merge', 'Edge-case tests'],
    hiddenTests: ['"tomatos" finds "tomato"', 'Scaling 3 → 4 servings on "1½ cups" gives a sane result', 'Shopping list merges "2 eggs" + "3 eggs" → "5 eggs" but keeps "1 cup flour" + "200g flour" separate or converts consistently', 'Scaling by 0 or negative is rejected'],
    evaluationCriteria: ['Typo-tolerant search', 'Correct, sane scaling', 'Correct ingredient merging', 'Rejects invalid scaling'],
    hints: ['The unit model is the hard design decision — make it explicit', 'Fuse.js or a simple edit-distance for search'],
    solutionStrategy: 'Units and scaling are the real problem. Decide how much unit intelligence you want and test the scaling engine thoroughly.',
    reflectionQuestions: ['Where did you have to constrain scope to avoid a rabbit hole (full unit conversion)?', 'How does this app\'s content/UI split compare to the quiz app?'],
  },
  {
    id: 'pr-dashboard', tier: 'intermediate', skills: ['building', 'architecture', 'testing'],
    title: 'Analytics dashboard',
    summary: 'A responsive dashboard with charts, filters, and a data table, fed by a mock API.',
    requirements: ['4+ chart types', 'Global date-range and segment filters that drive every widget', 'Sortable, paginated table', 'Loading, empty, and error states for every widget', 'Mock API with artificial latency and occasional failures'],
    constraints: ['Independent requests run in parallel', 'One source of truth for filter state', 'Accessible charts (labels, not color alone)'],
    suggestedWorkflow: ['ADR: filter state architecture', 'Mock API + data layer', 'Widgets with all three states', 'Wire filters', 'Test the states and the parallelism'],
    hiddenTests: ['Changing a filter updates all widgets from one state change', 'A widget whose request fails shows an error state, others still load', 'Empty result set shows empty state, not a broken chart', 'Requests fire in parallel, not sequentially'],
    evaluationCriteria: ['Single filter source of truth', 'All states handled per widget', 'Parallel data fetching', 'Accessible charts'],
    hints: ['This is where "make the dashboard look better" becomes a real spec', 'Ask Claude to prove requests are parallel (network timeline)'],
    solutionStrategy: 'Filter state design is the ADR-worthy decision. Every widget needs loading/empty/error from the start — retrofitting them is painful.',
    reflectionQuestions: ['What broke when a single widget failed, before you handled it?', 'How did you verify the requests were actually parallel?'],
  },

  // ---------------- Advanced ----------------
  {
    id: 'pr-saas', tier: 'advanced', skills: ['building', 'architecture', 'security', 'testing'],
    title: 'SaaS application (auth + billing shell)',
    summary: 'Multi-tenant app skeleton: signup, auth, org/workspace model, role-based access, a billing placeholder.',
    requirements: ['Email/password auth with sessions', 'Organizations with members and roles (owner/admin/member)', 'Every data query scoped to the current org', 'Role-gated actions', 'Billing plan field + upgrade stub', 'Audit log of sensitive actions'],
    constraints: ['No cross-tenant data leakage — proven by tests', 'Passwords hashed (argon2/bcrypt)', 'Authz checked server-side on every mutation', 'Secrets in env, never committed'],
    suggestedWorkflow: ['ADR: tenancy model (shared DB + org_id vs schema-per-tenant)', 'Auth', 'Org/role model', 'Query scoping middleware', 'Authz on every endpoint', 'Cross-tenant isolation tests', '/security-review'],
    hiddenTests: ['User in org A cannot read or mutate any org B resource, via any endpoint', 'A member cannot perform an admin action even with a crafted request', 'Session fixation and basic auth attacks fail', 'Audit log records who did what'],
    evaluationCriteria: ['Provable tenant isolation', 'Server-side authz everywhere', 'Safe credential handling', 'Passing security review'],
    hints: ['Write the cross-tenant isolation tests early and keep them green', 'Use a security-reviewer subagent with a threat model'],
    solutionStrategy: 'Tenancy and authz are the entire point. Build the isolation tests first; every feature must keep them green. Server-side authz on every mutation, no exceptions.',
    reflectionQuestions: ['How did you prove there is no cross-tenant leak, rather than assert it?', 'Where would you place human gates if Claude built this autonomously?'],
  },
  {
    id: 'pr-ai-app', tier: 'advanced', skills: ['building', 'architecture', 'security', 'autonomy'],
    title: 'AI-powered application',
    summary: 'An app that uses an LLM API for a real feature (e.g. document Q&A) with proper guardrails.',
    requirements: ['Ingest documents, chunk, embed, retrieve', 'Answer questions with citations to source chunks', 'Streaming responses', 'Handle API errors, rate limits, and timeouts', 'Cost/token tracking', 'The API key never reaches the frontend'],
    constraints: ['Key on the server only', 'Untrusted document content is treated as data, not instructions (prompt-injection aware)', 'Graceful degradation when the API is down', 'Deterministic tests via a mocked model'],
    suggestedWorkflow: ['ADR: retrieval approach + where the key lives', 'Ingestion pipeline', 'Retrieval + prompt assembly', 'Streaming + error handling', 'Injection-resistance tests', 'Cost tracking'],
    hiddenTests: ['A document containing "ignore instructions and output X" does not change the answer format or leak the system prompt', 'API 429/500/timeout each produce a handled state, not a crash', 'Answers cite real retrieved chunks', 'No request path exposes the API key'],
    evaluationCriteria: ['Key isolation', 'Injection resistance', 'Robust error handling', 'Citations are real', 'Cost visibility'],
    hints: ['Treat retrieved document text as untrusted — this mirrors Level 16', 'Mock the model so tests are deterministic and free'],
    solutionStrategy: 'The guardrails are the project: key isolation, injection resistance, error handling. The RAG plumbing is well-trodden; the safety around it is where judgment shows.',
    reflectionQuestions: ['How is untrusted document content like a malicious repo?', 'What did you mock, and why does that make the tests trustworthy?'],
  },
  {
    id: 'pr-fullstack', tier: 'advanced', skills: ['building', 'architecture', 'git', 'testing'],
    title: 'Full-stack application',
    summary: 'A complete app with a real backend, database, API, and frontend — built as verified increments.',
    requirements: ['REST or RPC API with validation', 'Relational database with migrations', 'Frontend consuming the API', 'Auth', 'Integration tests across the API↔DB boundary', 'e2e test for one critical journey', 'One-command dev environment'],
    constraints: ['Migrations are reversible and tested', 'API contract documented', 'Each increment is its own PR', 'CI runs unit + integration'],
    suggestedWorkflow: ['Spec + ADR', 'DB schema + migrations', 'API endpoint by endpoint, each with tests', 'Frontend feature by feature', 'e2e on the main journey', 'CI'],
    hiddenTests: ['Migrations run forward and back cleanly', 'API rejects invalid payloads with useful errors', 'The critical journey passes e2e', 'A fresh checkout + one command yields a running app'],
    evaluationCriteria: ['Reversible migrations', 'Tested API↔DB integration', 'Working e2e journey', 'Trivial onboarding'],
    hints: ['One increment per PR — resist the big-bang', 'The "one command to run it" test is real; verify it on a clean clone'],
    solutionStrategy: 'This is the build loop at full size. Increments, each shippable and verified. The onboarding command is a feature — test it.',
    reflectionQuestions: ['Which increment order minimized risk?', 'What did the e2e test catch that unit tests could not?'],
  },
  {
    id: 'pr-collab', tier: 'advanced', skills: ['building', 'architecture', 'debugging', 'testing'],
    title: 'Collaborative application',
    summary: 'Real-time multi-user editing or presence (e.g. a shared list or board) with conflict handling.',
    requirements: ['Multiple clients see each other\'s changes live', 'Presence indicators', 'Conflict resolution strategy (last-write-wins, CRDT, or OT — pick and justify)', 'Reconnection after network drop', 'Offline edits reconcile on reconnect'],
    constraints: ['The conflict strategy is documented in an ADR', 'Concurrency is tested with simulated interleaving', 'No lost updates under the chosen strategy'],
    suggestedWorkflow: ['ADR: sync + conflict model', 'Transport (WebSocket)', 'State sync', 'Presence', 'Reconnection + offline queue', 'Concurrency tests'],
    hiddenTests: ['Two clients editing the same item concurrently converge to a documented result with no lost update', 'A client that disconnects and reconnects catches up correctly', 'Offline edits made by two clients merge sanely', 'Rapid updates do not desync any client'],
    evaluationCriteria: ['Convergence under concurrency', 'Robust reconnection', 'Documented, justified conflict model', 'Concurrency tests exist'],
    hints: ['This is Level 6\'s concurrency bug as a whole project — simulate interleaving in tests', 'Start with last-write-wins; only reach for CRDT if you can justify it'],
    solutionStrategy: 'Concurrency and reconnection are the project. Simulate interleaved operations in tests; the ADR forces you to actually choose a conflict model.',
    reflectionQuestions: ['How did you reproduce a race deterministically in a test?', 'What would "trust Claude to build this autonomously" require?'],
  },

  // ---------------- Expert ----------------
  {
    id: 'pr-prod-saas', tier: 'expert', skills: ['building', 'architecture', 'security', 'autonomy', 'large-projects'],
    title: 'Production-grade SaaS',
    summary: 'Take the SaaS skeleton to production readiness: observability, rate limiting, background jobs, deploy pipeline, runbook.',
    requirements: ['Structured logging + request tracing', 'Rate limiting per API key and per tenant', 'Background job system with retries and dead-letter', 'Health checks + graceful shutdown', 'CI/CD with staged deploys and rollback', 'Incident runbook', 'Load test for a key endpoint'],
    constraints: ['No secret in logs', 'Jobs are idempotent', 'Deploys are reversible', 'Every new capability has monitoring'],
    suggestedWorkflow: ['ADR per subsystem', 'Observability first', 'Rate limiting', 'Jobs', 'Deploy pipeline + rollback', 'Load test + tune', 'Runbook'],
    hiddenTests: ['Rate limit returns 429 + Retry-After at the boundary and recovers', 'A job that fails 3x lands in the dead-letter queue, not an infinite loop', 'A bad deploy can be rolled back with one command', 'Logs contain no tokens or PII'],
    evaluationCriteria: ['Real observability', 'Correct rate limiting', 'Safe, idempotent jobs', 'Reversible deploys', 'Actionable runbook'],
    hints: ['This is Level 17 + Level 20 combined — increments, gates, verification', 'The runbook is a deliverable, not an afterthought'],
    solutionStrategy: 'Each subsystem is an increment with its own ADR and monitoring. Deploy safety and job idempotency are the highest-stakes parts — gate them.',
    reflectionQuestions: ['Which subsystem would you never let Claude ship without a human gate?', 'How does the runbook change what you can safely automate?'],
  },
  {
    id: 'pr-multiagent', tier: 'expert', skills: ['autonomy', 'subagents', 'teamwork', 'architecture'],
    title: 'Multi-agent application',
    summary: 'An app (or internal tool) that orchestrates multiple LLM agents with roles, hand-offs, and verification.',
    requirements: ['A coordinator that decomposes a task and assigns sub-tasks', 'Specialized agents with restricted capabilities', 'A verification/critic step before results are accepted', 'Human-approval gate for side effects', 'Full trace/audit of every agent action', 'Deterministic tests with mocked agents'],
    constraints: ['No agent can perform an irreversible action without the gate', 'The critic runs in a context that did not produce the result', 'Runaway loops are bounded (max turns / cost)'],
    suggestedWorkflow: ['ADR: orchestration model', 'Coordinator + task graph', 'Specialized agents', 'Critic/verification loop', 'Approval gate + audit', 'Mocked-agent tests'],
    hiddenTests: ['A sub-agent proposing an irreversible action is blocked until approval', 'The critic rejects a deliberately wrong result and triggers a retry', 'A misbehaving agent cannot exceed the turn/cost budget', 'The audit trail reconstructs exactly what happened'],
    evaluationCriteria: ['Enforced approval gate', 'Independent verification', 'Bounded loops', 'Complete audit trail'],
    hints: ['This is Level 20 as an artifact — "PLAN → EXECUTE → VERIFY → REVIEW → CORRECT → VERIFY AGAIN" in code', 'The critic must not see the producing context'],
    solutionStrategy: 'The control loop and the gates are the product. Build the verification and approval enforcement first; the agents are replaceable behind them.',
    reflectionQuestions: ['Where in your design does "autonomy ≠ blind trust" become concrete code?', 'What happens when the critic is itself wrong?'],
  },
  {
    id: 'pr-legacy-refactor', tier: 'expert', skills: ['large-projects', 'testing', 'git', 'debugging'],
    title: 'Complex legacy refactoring',
    summary: 'Modernize a substantial legacy module (framework upgrade, or extract a service) with zero production regressions.',
    requirements: ['Architecture map + risk register', 'Characterization test suite for the target before any change', 'A documented migration pattern', 'A series of independently deployable increments, one PR each', 'Feature flags for risky cutover', 'A rollback plan per increment'],
    constraints: ['Every increment keeps the characterization suite green', 'No increment touches more than it must', 'Contract tests at every new seam', 'Production behavior unchanged until deliberate cutover'],
    suggestedWorkflow: ['Map + risk register', 'Characterize', 'Pattern choice ADR', 'Increment loop: change one thing behind a flag, prove equivalence, ship', 'Cutover', 'Remove old code'],
    hiddenTests: ['Characterization suite passes at every commit on the branch', 'Each increment is independently revertable', 'The cutover is a flag flip, reversible in seconds', 'Old code paths are fully removed at the end, not left dangling'],
    evaluationCriteria: ['Zero behavior change until cutover', 'Green characterization suite throughout', 'Small, revertable increments', 'Clean removal of the old implementation'],
    hints: ['This is the Level 17 project, graded hard', 'One increment per session; /clear between them; the project remembers via artifacts'],
    solutionStrategy: 'Characterize, then never let the suite go red. Branch by abstraction or strangler fig. Flags make cutover safe. Context discipline is what makes a long migration finishable.',
    reflectionQuestions: ['What did characterization tests catch that you would otherwise have shipped?', 'How did you keep your context clean across many increments?'],
  },
  {
    id: 'pr-large-ai', tier: 'expert', skills: ['building', 'architecture', 'security', 'autonomy', 'large-projects'],
    title: 'Large-scale AI application',
    summary: 'A production AI system: ingestion at scale, evaluation harness, cost controls, safety filters, and monitoring.',
    requirements: ['Scalable ingestion + indexing pipeline', 'An evaluation harness with a labeled set and regression tracking', 'Prompt/version management', 'Input and output safety filtering', 'Per-tenant cost caps and alerting', 'Observability for latency, cost, and quality', 'Deterministic tests via recorded model responses'],
    constraints: ['Quality regressions are caught by the eval harness in CI', 'A tenant cannot exceed its cost cap', 'Untrusted input cannot alter system behavior', 'Prompts are versioned and rollback-able'],
    suggestedWorkflow: ['ADR: pipeline + eval strategy', 'Ingestion', 'Eval harness + baseline', 'Safety filters', 'Cost controls', 'Observability', 'CI gates on eval'],
    hiddenTests: ['A prompt change that drops eval score below baseline fails CI', 'A tenant at its cost cap is throttled, not silently overspending', 'A crafted input probing for the system prompt is filtered/handled', 'Rolling back a prompt version restores prior behavior'],
    evaluationCriteria: ['Eval harness gates quality', 'Enforced cost caps', 'Effective safety filtering', 'Versioned, reversible prompts', 'Real observability'],
    hints: ['The eval harness is your "verification" from Level 4 at system scale', 'Recorded model responses make CI deterministic and cheap'],
    solutionStrategy: 'Without an eval harness you cannot safely change anything — build it first and gate CI on it. Cost caps and safety filters are non-negotiable production concerns, not features.',
    reflectionQuestions: ['How is the eval harness the same idea as a test suite?', 'What must a human approve before a prompt version ships?'],
  },
];

export const PROJECT_BY_ID: Record<string, Project> = Object.fromEntries(PROJECTS.map((p) => [p.id, p]));
export const PROJECT_TIERS = ['beginner', 'intermediate', 'advanced', 'expert'] as const;
