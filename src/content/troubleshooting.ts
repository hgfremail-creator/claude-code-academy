import type { TroubleshootEntry } from './types';

export const TROUBLESHOOTING: TroubleshootEntry[] = [
  {
    id: 'ts-install-1',
    category: 'Installation',
    symptom: 'The install command fails with "syntax error near unexpected token" or a 403.',
    causes: ['Running the PowerShell command in CMD (or vice versa)', 'A proxy or firewall blocking the download', 'Shell mismatch — `irm | iex` is PowerShell, `curl | bash` is bash/WSL'],
    diagnostics: ['Check your prompt: `PS C:\\>` means PowerShell, `C:\\>` means CMD', 'Try downloading the script to a file and running it separately', 'Check corporate proxy settings'],
    solution: 'Use the installer matching your shell. macOS/Linux/WSL: `curl -fsSL https://claude.ai/install.sh | bash`. Windows PowerShell: `irm https://claude.ai/install.ps1 | iex`. Or use a package manager: Homebrew, WinGet, apt/dnf/apk. See the official install-troubleshooting page to match your exact error.',
    prevention: 'Confirm which shell you are in before pasting an installer. Prefer package-manager installs on managed machines.',
  },
  {
    id: 'ts-install-2',
    category: 'Installation',
    symptom: 'Claude Code uses PowerShell instead of Bash on Windows.',
    causes: ['Git for Windows is not installed', 'Git is installed but not on PATH'],
    diagnostics: ['Run `git --version` in a fresh terminal', 'Check whether `bash` is resolvable'],
    solution: 'Install Git for Windows (which bundles Bash). Claude Code then uses the Bash tool; without it, it falls back to PowerShell. WSL setups do not need Git for Windows.',
    prevention: 'Install Git for Windows as part of your standard dev setup on native Windows.',
  },
  {
    id: 'ts-auth-1',
    category: 'Authentication',
    symptom: 'Claude Code keeps asking me to approve an API key instead of showing the subscription login.',
    causes: ['ANTHROPIC_API_KEY is set in your environment or shell profile', 'A managed setting forces API auth'],
    diagnostics: ['`echo $ANTHROPIC_API_KEY` (bash) or `$env:ANTHROPIC_API_KEY` (PowerShell)', 'Run `claude auth status`', 'Check `~/.claude/settings.json` and managed settings'],
    solution: 'If you want subscription billing, unset ANTHROPIC_API_KEY (remove it from your shell profile too) and run `claude auth login`. If you want API billing, approving the key is correct.',
    prevention: 'Keep API keys out of your global shell profile; scope them to the project that needs them.',
  },
  {
    id: 'ts-auth-2',
    category: 'Authentication',
    symptom: 'The trust prompt reappears every time I launch Claude Code.',
    causes: ['You are starting Claude Code directly in your home directory — trust is held for the session only there and not written to disk'],
    diagnostics: ['Check your working directory when launching (`pwd`)'],
    solution: 'Start Claude Code from a project subdirectory instead of your home directory. Trust acceptance is saved per directory there.',
    prevention: 'Always `cd` into the actual project before running `claude`.',
  },
  {
    id: 'ts-perm-1',
    category: 'Permissions',
    symptom: 'Claude Code asks for approval constantly and it is slowing me down.',
    causes: ['Manual mode with no allowlist', 'Repeating the same safe commands (lint, test, git status)'],
    diagnostics: ['Note which commands you keep approving', 'Check your plan — auto mode is the default starting mode on Pro/Max/Team for terminal and VS Code'],
    solution: 'Run `/permissions` and add narrow allow rules for the safe commands you trust (e.g. `Bash(npm run test:*)`, `Bash(git commit *)`). Enable sandboxing with `/sandbox` so isolated commands run without asking. Consider `--permission-mode acceptEdits` for a reviewed batch of edits.',
    prevention: 'Build a project `.claude/settings.json` allowlist and commit it for the team.',
  },
  {
    id: 'ts-perm-2',
    category: 'Permissions',
    symptom: 'Claude refuses to read or edit a file that is clearly part of my project.',
    causes: ['The file is outside the working-directory boundary', 'A deny rule matches it', 'A path-scoped hook is blocking it'],
    diagnostics: ['Check the path relative to where you launched `claude`', 'Run `/permissions` and look for deny rules', 'Run `/hooks` to see configured hooks'],
    solution: 'If the file is in a sibling directory, relaunch from a common parent or use `--add-dir`. If a deny rule or hook is blocking it intentionally (e.g. `db/migrations/`), that is working as designed — override deliberately, not reflexively.',
    prevention: 'Launch Claude Code from the repo root, not a deep subfolder, for multi-area work.',
  },
  {
    id: 'ts-term-1',
    category: 'Terminal',
    symptom: 'A chained command works for me but fails when Claude runs it on Windows.',
    causes: ['`&&` and `||` are not valid statement separators in Windows PowerShell', 'Unix-only commands (head, tail, which, touch) used in PowerShell'],
    diagnostics: ['Read the exact command Claude proposed', 'Check whether it assumes bash syntax'],
    solution: 'Ask Claude to use PowerShell-native syntax: `a; if ($?) { b }` instead of `a && b`; `Get-Content -TotalCount N` instead of `head`. Or install Git for Windows so Claude can use the Bash tool.',
    prevention: 'Note your OS/shell in CLAUDE.md: "Windows/PowerShell — no `&&` chaining".',
  },
  {
    id: 'ts-git-1',
    category: 'Git',
    symptom: 'Claude changed many files and I do not know what is safe to commit.',
    causes: ['A broad task touched more than expected', 'Unrelated formatting or lockfile churn mixed in'],
    diagnostics: ['Ask: "summarize the changed files grouped by purpose; flag anything not required for the task"', 'Run `git diff` and read each file', 'Look for `.env`, debug logs, commented-out code'],
    solution: 'Have Claude split the changes into coherent commits (one logical change each), revert anything unrelated, and only then open a PR. Review the diff in a fresh session or with `/code-review` for an unbiased pass.',
    prevention: 'Branch before non-trivial work; keep each task scoped; ask for "only the targeted change".',
  },
  {
    id: 'ts-git-2',
    category: 'Git',
    symptom: 'I hit /rewind but changes Claude made are still there.',
    causes: ['Those changes were made by a Bash command or external process, not Claude\'s file-editing tools', 'Checkpoints only track edit-tool changes'],
    diagnostics: ['Recall whether the change came from an Edit/Write or a shell command (e.g. `sed`, a script, `npm`)'],
    solution: 'Use Git to revert Bash-made changes: `git checkout -- <file>` or `git restore`. Checkpointing is in-session undo for edit tools only.',
    prevention: 'Commit or stash before letting Claude run scripts that modify files; treat branches as the real safety net.',
  },
  {
    id: 'ts-context-1',
    category: 'Context',
    symptom: 'Claude is forgetting earlier instructions and making more mistakes as the session goes on.',
    causes: ['Context window is filling up', 'Context pollution from unrelated tasks or failed approaches'],
    diagnostics: ['Run `/context` to check usage', 'Count how many times you have corrected the same point'],
    solution: 'If tasks are unrelated: `/clear`. If it is one long task: `/compact` (optionally with a focus instruction). If you have corrected the same thing 2+ times: `/clear` and restart with a sharper prompt that bakes in what you learned. Persist important state to a NOTES.md first.',
    prevention: 'One task per session; `/clear` between tasks; delegate research to subagents; use `/btw` for side questions.',
  },
  {
    id: 'ts-context-2',
    category: 'Context',
    symptom: 'An instruction I gave in chat disappeared after /compact.',
    causes: ['Conversation-only instructions are not guaranteed to survive compaction', 'It was summarized away'],
    diagnostics: ['Check whether the instruction is in CLAUDE.md or only in chat history'],
    solution: 'Put durable instructions in CLAUDE.md — project-root CLAUDE.md is re-read from disk after compaction. You can also tell Claude in CLAUDE.md what to always preserve when compacting.',
    prevention: 'If an instruction matters beyond one message, it belongs in CLAUDE.md or a written doc, not chat.',
  },
  {
    id: 'ts-claudemd-1',
    category: 'Context',
    symptom: 'Claude is not following my CLAUDE.md.',
    causes: ['The file did not load (wrong location, or excluded)', 'The file is too long and the rule is lost in noise', 'The instruction is vague or conflicts with another'],
    diagnostics: ['Run `/context` and check the "Memory files" list', 'Check the file is in a loaded location', 'Look for contradictory instructions across CLAUDE.md files and rules'],
    solution: 'Verify it loaded via `/context`. Prune aggressively (target under 200 lines) so key rules stand out. Make instructions specific and verifiable ("use 2-space indentation" not "format nicely"). Add IMPORTANT to the single rule that keeps being missed. For a must-happen-every-time rule, use a hook instead.',
    prevention: 'Treat CLAUDE.md like code: review when things go wrong, prune regularly, test whether edits change behavior. Run `/doctor` for trim suggestions.',
  },
  {
    id: 'ts-perf-1',
    category: 'Performance',
    symptom: 'Responses are slow or the session feels sluggish.',
    causes: ['Very large context window', 'A big CLAUDE.md or many files read into context', 'High effort setting for a simple task'],
    diagnostics: ['Run `/context` and `/cost`', 'Check effort/model settings with `/model`'],
    solution: 'Compact or clear context. Trim CLAUDE.md. Lower `--effort` for routine work; reserve high effort for genuinely hard problems. Use subagents so large reads happen off your main thread.',
    prevention: 'Keep sessions focused and short-lived; scope file reads.',
  },
  {
    id: 'ts-mcp-1',
    category: 'MCP',
    symptom: 'An MCP server I added does not appear or its tools are missing.',
    causes: ['Connection failed', 'Needs authentication', 'Syntax error in .mcp.json', 'Missing environment variable', 'Leading/trailing whitespace in a config value from pasting'],
    diagnostics: ['Run `/mcp` for status (connected / needs auth / failed)', 'Run `claude mcp list` for health and errors', 'Run `claude mcp get <name>` to inspect config', 'Check env vars referenced with `${VAR}`'],
    solution: 'Re-authenticate via `/mcp` for OAuth servers. Fix `.mcp.json` syntax and set missing env vars (or add `${VAR:-default}`). Remove hidden whitespace by re-running `claude mcp add` or editing the file. Ensure the same server is not configured in multiple scopes with different endpoints.',
    prevention: 'Add servers via `claude mcp add` rather than hand-editing; keep secrets in env vars; commit a clean project-scoped `.mcp.json`.',
  },
  {
    id: 'ts-mcp-2',
    category: 'MCP',
    symptom: 'An MCP tool is available but every call says it needs approval, or it is blocked entirely.',
    causes: ['Your organization marked the tool as "ask" or "blocked" via managed MCP settings', 'A permission rule requires approval'],
    diagnostics: ['Check `/permissions`', 'Ask your admin whether the tool needs organization approval'],
    solution: 'Request organization approval for the tool if it is blocked. Add an explicit allow rule for tools you use often and trust. Tools marked "ask" by policy will prompt on every call by design.',
    prevention: 'Coordinate with your admin on the managed MCP allowlist for tools your team relies on.',
  },
  {
    id: 'ts-hooks-1',
    category: 'Hooks',
    symptom: 'My hook is not running.',
    causes: ['Workspace not trusted (project frontmatter hooks require trust)', 'Matcher does not match the tool name', 'Wrong settings file / scope', 'Syntax error in settings.json', 'Managed policy restricts hooks to managed settings only'],
    diagnostics: ['Run `/hooks` to browse configured hooks and their sources', 'Enable debug logging to inspect hook input/output', 'Check the matcher against the actual tool name (e.g. `Edit|Write`, `Bash`, `mcp__server__.*`)'],
    solution: 'Trust the workspace. Fix the matcher. Confirm the hook is in a loaded settings file (`.claude/settings.json` for project). Validate JSON. Use the `InstructionsLoaded` hook or `/hooks` to confirm it is recognized.',
    prevention: 'Let Claude write the hook and settings entry, then verify with `/hooks` before relying on it.',
  },
  {
    id: 'ts-hooks-2',
    category: 'Hooks',
    symptom: 'A Stop hook is keeping the turn from ever finishing.',
    causes: ['The check the hook runs never passes', 'The hook script itself errors'],
    diagnostics: ['Run the hook\'s command manually and read its output/exit code', 'Check whether exit 2 is being returned unconditionally'],
    solution: 'Fix the underlying check or the script. Note that Claude Code overrides a Stop hook and ends the turn after ~8 consecutive blocks so you are not stuck forever. Temporarily disable the hook in settings while debugging.',
    prevention: 'Test hook scripts standalone; make failure messages actionable so Claude can fix the real problem.',
  },
  {
    id: 'ts-agents-1',
    category: 'Agents',
    symptom: 'My custom subagent is never used, or Claude Code skips its file.',
    causes: ['Missing `name` or `description` frontmatter (Claude Code skips such files)', 'Description too vague for Claude to know when to delegate', 'File in the wrong directory'],
    diagnostics: ['Run `/agents` to see which subagents are recognized', 'Check the file is in `.claude/agents/` or `~/.claude/agents/`', 'Read the frontmatter'],
    solution: 'Ensure both `name` (lowercase-hyphens) and `description` (a clear "use this when…") are present. @-mention the agent to force it. Store project agents in `.claude/agents/` and commit them.',
    prevention: 'Keep descriptions short and specific; combined agent descriptions over ~15k tokens trigger a warning.',
  },
  {
    id: 'ts-agents-2',
    category: 'Agents',
    symptom: 'A subagent gave a confused answer because it did not know the context I discussed earlier.',
    causes: ['Regular subagents start fresh — they do not see your conversation, only the task brief'],
    diagnostics: ['Recall what context the task actually required and whether the brief included it'],
    solution: 'Put everything the subagent needs into the delegation prompt, or reference files with `@`. For work that genuinely needs the full conversation, use a fork (inherits parent context) or keep the task in the main session.',
    prevention: 'If a task cannot be captured in a short self-contained brief, it is a poor fit for a regular subagent.',
  },
  {
    id: 'ts-test-1',
    category: 'Tests',
    symptom: 'Tests pass individually but fail when the whole suite runs.',
    causes: ['Order-dependent state pollution — shared DB rows, globals, mocks, or the filesystem not reset between tests', 'Tests relying on execution order'],
    diagnostics: ['Run the failing test alone, then with the preceding tests', 'Ask Claude to identify shared mutable state between the tests'],
    solution: 'Add proper setup/teardown that resets shared state (truncate tables, reset mocks, clear caches) between tests. Make each test independent of order.',
    prevention: 'Design tests to be isolated from the start; prefer per-test fixtures over shared module-level state.',
  },
  {
    id: 'ts-test-2',
    category: 'Tests',
    symptom: 'Claude "fixed" the bug but the fix just makes the failing test pass without addressing the cause.',
    causes: ['Claude weakened an assertion, added a special case, or suppressed an error', 'The prompt did not forbid symptom suppression'],
    diagnostics: ['Diff the test file — did the assertions change?', 'Check for new try/catch, retries, or loosened types around the fix'],
    solution: 'Reject the change. Re-prompt: "address the root cause, do not modify the test to pass, do not suppress errors. Show me the ranked hypotheses and evidence first." Keep the original failing test as the regression guard.',
    prevention: 'Always state "root cause, not symptom" and "do not change the test" in debugging prompts.',
  },
  {
    id: 'ts-deploy-1',
    category: 'Deployment',
    symptom: 'It works locally but breaks in staging/production.',
    causes: ['Environment mismatch (runtime versions, env vars, feature flags)', 'Data shape differences (nulls, unicode, timezones, large rows)', 'Scale effects (connection pools, timeouts, memory)'],
    diagnostics: ['Diff runtime versions and env vars between local and the broken environment', 'Get the real production log/stack trace', 'Check for data the fixtures lack'],
    solution: 'Feed Claude the actual production error. Reproduce locally with prod-like data and the same environment. Fix the root cause and add a test. Use feature flags so a bad deploy is a toggle, not a rollback.',
    prevention: 'Keep environments as similar as possible; seed local/CI with anonymized realistic data; gate risky changes behind flags.',
  },
  {
    id: 'ts-win-1',
    category: 'Windows',
    symptom: 'Claude Code triggers unexpected network requests or accesses strange `\\\\` paths.',
    causes: ['WebDAV is enabled, and Claude Code accessed a path like `\\\\*` that maps to a WebDAV subdirectory'],
    diagnostics: ['Check whether WebDAV / the WebClient service is enabled', 'Review the paths Claude Code was allowed to access'],
    solution: 'Do not enable WebDAV, and do not allow Claude Code to access `\\\\*`-style paths. WebDAV is deprecated by Microsoft for security reasons and can bypass the permission system.',
    prevention: 'Keep WebDAV disabled on machines running Claude Code; scope working directories to local paths.',
  },
  {
    id: 'ts-mac-1',
    category: 'macOS',
    symptom: 'Homebrew install of Claude Code is stuck on an old version.',
    causes: ['Homebrew installs do not auto-update', 'You installed the stable `claude-code` cask, which lags the latest channel'],
    diagnostics: ['`claude --version`', '`brew info claude-code` / `brew info claude-code@latest`'],
    solution: 'Run `brew upgrade claude-code` (or `brew upgrade claude-code@latest`). Switch to the native installer for background auto-updates, or the `claude-code@latest` cask for the newest releases.',
    prevention: 'Prefer the native install for auto-updates, or add `brew upgrade` to a weekly routine.',
  },
  {
    id: 'ts-linux-1',
    category: 'Linux',
    symptom: 'Credentials are not persisting between sessions on Linux.',
    causes: ['No system keychain available; falling back to file-based storage with permission issues', 'Home directory on a network mount'],
    diagnostics: ['Run `claude auth status`', 'Check permissions on `~/.claude/`', 'Check whether `$HOME` is local'],
    solution: 'Ensure `~/.claude/` is writable and on a local filesystem. Credentials are protected by file permissions on Linux when no keychain is present. Re-run `claude auth login`.',
    prevention: 'Run Claude Code with a local, correctly-permissioned home directory; use `claude setup-token` for long-lived automation tokens.',
  },
];

export const TS_CATEGORIES = Array.from(new Set(TROUBLESHOOTING.map((t) => t.category)));
