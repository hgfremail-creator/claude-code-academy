import type { CommandEntry } from './types';

// Command Explorer data. Version-sensitive — cross-check the CLI reference and
// `/help` in your own session. https://code.claude.com/docs/en/cli-reference
export const COMMANDS: CommandEntry[] = [
  {
    id: 'claude',
    name: 'claude',
    category: 'starting',
    summary: 'Start an interactive Claude Code session in the current directory.',
    whatItDoes:
      'Launches an interactive agent session scoped to your working directory. On first run in a folder it asks you to trust it. Loads CLAUDE.md, settings, and configured MCP servers.',
    whenToUse: 'The normal way to begin work on a project in the terminal.',
    beginnerExample: 'cd my-project && claude',
    advancedExample: 'claude --permission-mode plan --model opus "audit our auth flow and propose hardening"',
    commonMistake:
      'Running it in your home directory. Trust is not persisted there and the working-directory boundary is huge. Start in the project folder.',
    related: ['claude-continue', 'claude-print', 'permission-mode'],
    versionSensitive: true,
  },
  {
    id: 'claude-print',
    name: 'claude -p / --print',
    category: 'automation',
    summary: 'Run one prompt non-interactively and exit.',
    whatItDoes:
      'Executes a single request without the interactive UI, prints the result, and exits. Supports --output-format text|json|stream-json for parsing. Still creates a resumable session unless --no-session-persistence.',
    whenToUse: 'CI pipelines, pre-commit hooks, scripts, piping data in, fan-out loops.',
    beginnerExample: 'claude -p "explain what this project does"',
    advancedExample:
      'git diff main --name-only | claude -p "review these files for security issues" --output-format json --allowedTools "Read,Grep"',
    commonMistake:
      'Using -p for exploratory work you will want to iterate on. Interactive sessions are better for anything with back-and-forth.',
    related: ['claude', 'batch', 'goal'],
    versionSensitive: true,
  },
  {
    id: 'claude-continue',
    name: 'claude -c / --continue, -r / --resume',
    category: 'session',
    summary: 'Resume a previous conversation.',
    whatItDoes:
      '-c resumes the most recent conversation in this directory. -r lets you pick from a list or resume by id/name. Conversations persist locally with their checkpoints.',
    whenToUse: 'Picking up a multi-sitting task without re-explaining context.',
    beginnerExample: 'claude -c',
    advancedExample: 'claude -r "oauth-migration" "address the PR review comments and push"',
    commonMistake:
      'Resuming a stale session for a new task. If the old context is irrelevant, start fresh — a polluted context hurts more than re-explaining helps.',
    related: ['clear', 'rewind', 'claude'],
    versionSensitive: true,
  },
  {
    id: 'context',
    name: '/context',
    category: 'context',
    summary: 'Show what is loaded into the context window and how full it is.',
    whatItDoes:
      'Displays the startup context breakdown: memory files (CLAUDE.md), system prompt, tools, and current usage. The fastest way to confirm a CLAUDE.md actually loaded.',
    whenToUse: 'When Claude ignores an instruction (did the file load?), or to gauge remaining room.',
    beginnerExample: '/context',
    advancedExample: 'Run /context after /compact to confirm what survived and how much room you regained.',
    commonMistake: 'Assuming a CLAUDE.md is loaded because it exists. Location and excludes matter — verify with /context.',
    related: ['compact', 'clear', 'memory'],
    versionSensitive: true,
  },
  {
    id: 'clear',
    name: '/clear',
    category: 'context',
    summary: 'Wipe the conversation context entirely.',
    whatItDoes: 'Resets the context window to a fresh state. CLAUDE.md reloads; conversation history is gone.',
    whenToUse: 'Switching to an unrelated task; after 2+ failed corrections on the same point.',
    beginnerExample: '/clear',
    advancedExample:
      'Pattern: finish a phase → "write what we decided to NOTES.md" → /clear → "continue from NOTES.md" — carrying state forward without the cruft.',
    commonMistake: 'Not clearing between unrelated tasks (the "kitchen sink session"), so context fills with irrelevant history.',
    related: ['compact', 'context', 'rewind'],
    versionSensitive: true,
  },
  {
    id: 'compact',
    name: '/compact [instructions]',
    category: 'context',
    summary: 'Summarize the conversation to reclaim space while keeping key state.',
    whatItDoes:
      'Replaces history with a summary that preserves important code, decisions, and file states. Optional instructions steer what to keep. Auto-triggers near the limit.',
    whenToUse: 'A long single task running low on room, where the history still matters.',
    beginnerExample: '/compact',
    advancedExample: '/compact focus on the API contract changes and the list of modified files; drop the debugging tangent',
    commonMistake:
      'Relying on compaction to preserve a chat-only instruction. Put durable instructions in CLAUDE.md — project-root CLAUDE.md is re-read after compaction.',
    related: ['clear', 'context', 'rewind'],
    versionSensitive: true,
  },
  {
    id: 'rewind',
    name: '/rewind (Esc Esc)',
    category: 'session',
    summary: 'Restore earlier conversation and/or code state from a checkpoint.',
    whatItDoes:
      'Opens the rewind menu. Every prompt is a checkpoint. Restore conversation only, code only, both, or "summarize from/up to here". Tracks Claude edit tools, NOT Bash changes.',
    whenToUse: 'A thread went down a dead end; an edit made things worse; you want to try a risky approach and be able to back out.',
    beginnerExample: 'Press Esc twice, pick a checkpoint, choose "Restore both".',
    advancedExample:
      '"Restore code only" to keep the discussion but revert files, then redirect the approach without re-explaining.',
    commonMistake: 'Treating checkpoints as a Git replacement. They do not capture Bash or external changes. Branch first.',
    related: ['clear', 'claude-continue', 'permission-mode'],
    versionSensitive: true,
  },
  {
    id: 'permission-mode',
    name: 'Shift+Tab / --permission-mode',
    category: 'config',
    summary: 'Switch between default, plan, acceptEdits, auto, and bypassPermissions.',
    whatItDoes:
      'Controls how much Claude Code asks before acting. Shift+Tab cycles mid-session; --permission-mode sets the start mode.',
    whenToUse:
      'plan for exploring/designing; default for unfamiliar code; acceptEdits for a reviewed batch of edits; auto for flow with a safety net; bypass only in throwaway sandboxes.',
    beginnerExample: 'Press Shift+Tab until the status bar shows "plan mode on".',
    advancedExample: 'claude --permission-mode auto -p "fix all lint errors and run the test suite until green"',
    commonMistake: 'Using bypassPermissions on a real machine to avoid prompts. That removes the only gate on destructive/exfiltrating commands.',
    related: ['permissions', 'claude', 'hooks'],
    versionSensitive: true,
  },
  {
    id: 'permissions',
    name: '/permissions',
    category: 'config',
    summary: 'View and edit allow/ask/deny rules for tools and commands.',
    whatItDoes:
      'Manages the permission allowlist. Pre-approve safe commands (npm run lint, git commit) so you stop clicking through prompts; deny dangerous ones outright.',
    whenToUse: 'After the tenth identical approval; when hardening a sensitive repo; to allowlist doc domains for web fetch.',
    beginnerExample: '/permissions then add an allow rule for `Bash(npm run test:*)`',
    advancedExample: 'Deny `Bash(curl *)` and `Bash(git push --force*)` in project settings, committed for the whole team.',
    commonMistake: 'Allowlisting broad patterns like `Bash(*)`. Scope rules narrowly; the point is to skip the safe ones, not all of them.',
    related: ['permission-mode', 'hooks', 'doctor'],
    versionSensitive: true,
  },
  {
    id: 'init',
    name: '/init',
    category: 'config',
    summary: 'Generate or improve a project CLAUDE.md by analyzing the codebase.',
    whatItDoes:
      'Explores the project and drafts a CLAUDE.md with build/test commands and discovered conventions. If one exists, suggests improvements instead of overwriting. Reads Cursor/Copilot rules too.',
    whenToUse: 'Starting Claude Code on a project that has no CLAUDE.md; onboarding a repo.',
    beginnerExample: '/init',
    advancedExample: 'CLAUDE_CODE_NEW_INIT=1 claude then /init for the interactive multi-phase flow (also offers skills and hooks).',
    commonMistake: 'Shipping the generated file as-is. It captures what Claude can discover; you still need to add the non-obvious gotchas and remove the derivable filler.',
    related: ['memory', 'context', 'doctor'],
    versionSensitive: true,
  },
  {
    id: 'memory',
    name: '/memory',
    category: 'config',
    summary: 'List, open, and edit CLAUDE.md files; toggle auto memory.',
    whatItDoes:
      'Shows all memory file locations (user + project), lets you open any in your editor, toggles auto memory on/off, and opens the auto-memory folder.',
    whenToUse: 'Editing instructions; auditing what auto memory has saved; turning auto memory off for a project.',
    beginnerExample: '/memory then select the project CLAUDE.md to edit it',
    advancedExample: 'Review `~/.claude/projects/<project>/memory/` files and prune stale `feedback` notes Claude accumulated.',
    commonMistake: 'Forgetting that /memory lists files that may not have loaded. Use /context to see what is actually in the session.',
    related: ['init', 'context', 'compact'],
    versionSensitive: true,
  },
  {
    id: 'code-review',
    name: '/code-review',
    category: 'review',
    summary: 'Run a bundled review of the current diff in a fresh subagent.',
    whatItDoes:
      'A fresh-context subagent reviews your working changes (or a target PR/branch) for correctness bugs and returns findings to the session. --comment posts to a PR; --fix applies findings.',
    whenToUse: 'Before merging; after a long unattended run; whenever a second, unbiased pass is worth it.',
    beginnerExample: '/code-review',
    advancedExample: '/code-review high --fix — broader coverage, then apply the surviving findings to the working tree.',
    commonMistake: 'Treating every finding as mandatory. A reviewer surfaces gaps by design; scope your response to correctness and requirements.',
    related: ['security-review', 'agents', 'run'],
    versionSensitive: true,
  },
  {
    id: 'security-review',
    name: '/security-review',
    category: 'review',
    summary: 'Run a security pass over the changes on your current branch.',
    whatItDoes: 'Reviews branch changes for common vulnerability classes (injection, authz, secrets, unsafe input handling) and reports findings.',
    whenToUse: 'Before merging anything touching auth, input handling, data access, or external calls.',
    beginnerExample: '/security-review',
    advancedExample: 'Pair with a security-reviewer subagent that has a project-specific threat model in its system prompt.',
    commonMistake: 'Assuming a clean report means "secure". It catches common classes, not everything. Human review of auth/data paths still matters.',
    related: ['code-review', 'agents'],
    versionSensitive: true,
  },
  {
    id: 'run',
    name: '/run and /verify',
    category: 'review',
    summary: 'Launch and drive your app, or build+run to confirm a change works.',
    whatItDoes:
      '/run starts your app the way this project expects and drives it. /verify builds and runs to confirm code changes against the real app. /run-skill-generator teaches them how to build your project.',
    whenToUse: 'After Claude\'s own checks pass, to confirm against the running app — not just the test suite.',
    beginnerExample: '/run',
    advancedExample: 'CI: rely on tests; locally: /verify after each increment so "green tests" is backed by "actually works".',
    commonMistake: 'Skipping the real-app check because tests pass. Tests can be green while the app is broken in a way no test covers.',
    related: ['code-review', 'claude-print'],
    versionSensitive: true,
  },
  {
    id: 'mcp',
    name: '/mcp  &  claude mcp add|list|get|remove|login',
    category: 'mcp',
    summary: 'Connect and manage Model Context Protocol servers.',
    whatItDoes:
      'claude mcp add wires up a server (stdio/http/sse) at a chosen scope (local/project/user). /mcp shows connection status and runs OAuth logins. .mcp.json shares project-scoped servers via git.',
    whenToUse: 'Giving Claude access to a database, issue tracker, docs, monitoring, or design tool.',
    beginnerExample: 'claude mcp add --transport http notion https://mcp.notion.com/mcp',
    advancedExample:
      'claude mcp add --transport stdio db -- npx -y @bytebase/dbhub --dsn "postgresql://readonly:...@host/analytics"  (read-only role, least privilege)',
    commonMistake:
      'Connecting a server that fetches external content without trusting the publisher — that content can carry prompt-injection payloads. Also: giving write/admin DB credentials when read-only would do.',
    related: ['permissions', 'agents', 'security-review'],
    versionSensitive: true,
  },
  {
    id: 'agents',
    name: '/agents  &  --agent / --agents',
    category: 'agents',
    summary: 'Create, inspect, and run custom subagents.',
    whatItDoes:
      'Subagents are defined in .claude/agents/*.md (frontmatter + system prompt). They run in isolated context with a restricted toolset and return a summary. Built-ins: Explore, Plan, general-purpose, claude.',
    whenToUse: 'Verbose investigation, diff review in fresh context, specialized roles (security, tests, docs), parallel fan-out.',
    beginnerExample: '"Use the Explore subagent to find every caller of deprecatedFn."',
    advancedExample:
      '@"security-reviewer (agent)" review the auth diff — @-mention guarantees that specific agent runs.',
    commonMistake:
      'Delegating tight-iteration work. Regular subagents start fresh (no conversation history), so the re-briefing overhead outweighs the isolation benefit.',
    related: ['code-review', 'worktrees', 'batch'],
    versionSensitive: true,
  },
  {
    id: 'hooks',
    name: '/hooks  &  hooks in settings.json',
    category: 'automation',
    summary: 'Run scripts deterministically at lifecycle events.',
    whatItDoes:
      'Configured in settings.json under "hooks". Events: PreToolUse, PostToolUse, Stop, SessionStart, etc. Exit 2 blocks the action. Handler types: command, http, mcp_tool, prompt, agent.',
    whenToUse: 'Things that must happen every time: format after edit, lint/test gate, block writes to protected paths, audit logging.',
    beginnerExample: 'PostToolUse matcher "Edit|Write" → run prettier on the changed file.',
    advancedExample: 'Stop hook runs the full test suite; exit 2 while failing so the turn cannot end until tests pass.',
    commonMistake: 'Putting a "must happen every time" rule in CLAUDE.md (advisory) instead of a hook (deterministic).',
    related: ['permissions', 'code-review', 'init'],
    versionSensitive: true,
  },
  {
    id: 'batch',
    name: '/batch <instruction>',
    category: 'automation',
    summary: 'Fan a change across 5–30 subagents, each in its own worktree opening a PR.',
    whatItDoes: 'Splits a mechanical, repetitive change across many parallel subagents. Each works in an isolated worktree and opens its own pull request.',
    whenToUse: 'Large migrations and codemods: "add types to these 300 files", "update the license header everywhere".',
    beginnerExample: '/batch update all import paths from "@old/pkg" to "@new/pkg"',
    advancedExample: 'Test the instruction on 2–3 files with claude -p first, refine, then /batch the full set.',
    commonMistake: 'Using it for changes that are not independent per-file — you get merge chaos across 20 PRs.',
    related: ['claude-print', 'agents', 'worktrees'],
    versionSensitive: true,
  },
  {
    id: 'goal',
    name: '/goal <condition>',
    category: 'automation',
    summary: 'Keep Claude working until an evaluator confirms a condition is met.',
    whatItDoes:
      'Sets a goal condition re-checked by a separate evaluator after every turn. Claude keeps iterating until it resolves; Claude Code eventually stops a stalled run.',
    whenToUse: 'A multi-step task you want to run mostly unattended with a clear definition of done.',
    beginnerExample: '/goal the test suite passes and `npm run typecheck` is clean',
    advancedExample: 'Combine with a Stop hook (deterministic gate) and a review subagent for layered assurance.',
    commonMistake: 'Setting a goal with no machine-checkable condition. "Looks good" is not a goal an evaluator can verify.',
    related: ['hooks', 'code-review', 'claude-print'],
    versionSensitive: true,
  },
  {
    id: 'worktrees',
    name: 'git worktree  &  isolation: worktree',
    category: 'agents',
    summary: 'Run parallel sessions in isolated checkouts of one repo.',
    whatItDoes:
      'git worktree add checks out another branch into a separate directory sharing one .git. Subagents can set isolation: worktree; the desktop app puts each parallel session in its own.',
    whenToUse: 'Two independent tasks at once; Writer/Reviewer split; fan-out migrations.',
    beginnerExample: 'git worktree add ../app-bugfix fix/login && cd ../app-bugfix && claude',
    advancedExample: 'One worktree per independent workstream; serialize anything where one task depends on another\'s output.',
    commonMistake: 'Parallelizing dependent work — you spend more time reconciling merges than you saved.',
    related: ['agents', 'batch', 'claude-continue'],
    versionSensitive: true,
  },
  {
    id: 'doctor',
    name: 'claude doctor / /doctor',
    category: 'config',
    summary: 'Run diagnostics on your installation and configuration.',
    whatItDoes: 'Checks version, install method, auth status, config problems, and proposes trims for a checked-in CLAUDE.md.',
    whenToUse: 'After install; when something behaves oddly; periodically to keep CLAUDE.md lean.',
    beginnerExample: 'claude doctor',
    advancedExample: 'Run /doctor in a session after adding rules/hooks to confirm they are recognized.',
    commonMistake: 'Ignoring the CLAUDE.md trim suggestions — a bloated file is a real cause of ignored instructions.',
    related: ['init', 'memory', 'context'],
    versionSensitive: true,
  },
  {
    id: 'help',
    name: '/help',
    category: 'starting',
    summary: 'The authoritative list of commands for YOUR installed version.',
    whatItDoes: 'Prints the slash commands and shortcuts available in your current version, plan, and surface.',
    whenToUse: 'Any time this Academy or the docs disagree with what you see — /help is ground truth locally.',
    beginnerExample: '/help',
    advancedExample: 'Skim /help after each auto-update; new commands appear frequently.',
    commonMistake: 'Memorizing a command list from a blog post. Availability varies by version/plan/surface — check /help.',
    related: ['doctor', 'context'],
    versionSensitive: true,
  },
];

export const COMMAND_BY_ID: Record<string, CommandEntry> = Object.fromEntries(COMMANDS.map((c) => [c.id, c]));
